[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "poker_native"
version = "0.1.0"
description = "Cœur MCCFR compilé (moteur de jeu, évaluateur, équité, traversée) pour PokerIA"
requires-python = ">=3.12"

[tool.maturin]
features = ["pyo3/extension-module"]
